<table>
  <thead>
    <tr>
      <th>Meeting</th>
      <th>Meeting Start Time</th>
      <th>Meeting End Time</th>
      <th>Arrival Time</th>
      <th>Departure Time</th>
    </tr>
  </thead>

  <tbody>
    <% attendances.each do |attendance| %>
      <tr>
        <td><%= link_to attendance.meeting.title_display, club_meeting_path(club, attendance.meeting) %></td>
        <td><%= attendance.meeting.start_time %></td>
        <td><%= attendance.arrival_time %></td>
        <td><%= attendance.departure_time %></td>
        <td><%= attendance.notes %></td>
        <td><%= link_to "Destroy", club_attendance_path(club, attendance), method: :delete, data: { confirm: "Are you sure?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
