<p>
  Title: <%= meeting.title %><br>
  Description: <%= meeting.description %><br>
  Start Time: <%= meeting.start_time %><br>
  End Time: <%= meeting.end_time %><br>
  Notes: <%= meeting.notes %>
</p>

<p><%= link_to "Edit", edit_club_meeting_path(club, meeting) %></p>

<h2><%= t(".attendees") %> &mdash; <%= meeting.attendances.count %></h2>

<%= render "meetings_attendances/attendances", club: club, attendances: meeting.attendances.includes(:attendee).limit(5) %>

<p><%= link_to "View All Attendees", club_meeting_attendances_path(club, meeting) %></p>

<p><%= link_to "Back", club_meetings_path(club) %></p>
